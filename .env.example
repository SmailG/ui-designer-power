# Google Gemini API Configuration
GEMINI_API_KEY=your_api_key_here

# Alternative: Use GOOGLE_API_KEY
# GOOGLE_API_KEY=your_api_key_here

# Optional: Use a Custom Gemini Gem (overrides all other model settings)
# GEMINI_GEM_ID=models/your-gem-id-here
# Get your Gem ID from Google AI Studio after creating a custom Gem
# When set, this Gem will be used for ALL operations (image + text generation)

# Optional: Specify Gemini Models (used when no custom Gem is set)
# GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview  # For UI design generation (default)
# GEMINI_MODEL=gemini-2.5-flash                  # For code generation (default)
# 
# Available IMAGE models:
# - gemini-3-pro-image-preview (Most advanced, thinking, up to 14 images - RECOMMENDED DEFAULT)
# - gemini-2.5-flash-image (Fast image generation, stable)
#
# Available TEXT models:
# - gemini-2.5-flash (Latest text model - RECOMMENDED DEFAULT)
# - gemini-2.5-pro (Most capable, slower, best quality)
# - gemini-3-pro-preview (Most intelligent, preview)
# - gemini-2.0-flash-exp (Fast, good quality)

# Optional: Vertex AI Configuration (for enterprise)
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Optional: Custom Gemini Gem ID (if using fine-tuned model)
# GEMINI_GEM_ID=your-gem-id
